<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title style="center">Do Something</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <nav>
        <div class="title">
            <img  id="Logo"
                src="/Images/Do-Something-1.png"
                alt="Pic. Logo"
                style="float:left;"
            />
            <h1>Do Something</h1>
               <label for="slogan">Your daily Task Manager</label>
        </div>
        <div>{{user_id}}</div>
        {{#if logged_in}}
            <a  href="/login" id="logout">Logout</a>
        {{else}}
            <a  href="/login">Login</a>
        {{/if}}
    </nav>
    <body>
        <script src="https://cdn.jsdelivr.net/npm/joi@17.7.1/dist/joi-browser.min.js"></script>
        {{{body}}}
        {{#if logged_in}}
            <script src="/js/logout.js"></script>
        {{/if}}
    </body>
</html>